{% extends 'front/base.html.twig' %}

{% block body %}
    <h1>{{ product.title }}</h1>
    <p><strong>Prix indicatif :</strong> {{ product.indicativePrice }} €</p>
    <p>{{ product.description }}</p>

    <p>
        <a href="{{ path('app_product_index') }}">← Retour au catalogue</a>
    </p>
{% endblock %}
