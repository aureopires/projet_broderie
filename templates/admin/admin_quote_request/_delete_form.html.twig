<form method="post" action="{{ path('admin_quote_request_delete', {'id': quote_request.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quote_request.id) }}">
    <button class="btn">Supprimer</button>
</form>
