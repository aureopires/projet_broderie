<form method="post" action="{{ path('admin_review_delete', {'id': review.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review.id) }}">
    <button class="btn">Supprimer</button>
</form>
