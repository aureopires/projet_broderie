<IfModule mod_rewrite.c>
    RewriteEngine On

    # Let Apache serve existing files such as compiled assets and uploads.
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
</IfModule>
